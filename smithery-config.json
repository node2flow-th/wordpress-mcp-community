{
  "type": "object",
  "properties": {
    "WORDPRESS_URL": {
      "type": "string",
      "description": "Your WordPress site URL (e.g. https://your-site.com)"
    },
    "WORDPRESS_USERNAME": {
      "type": "string",
      "description": "WordPress username"
    },
    "WORDPRESS_APP_PASSWORD": {
      "type": "string",
      "description": "WordPress Application Password (generate in Users → Profile → Application Passwords)"
    }
  },
  "required": ["WORDPRESS_URL", "WORDPRESS_USERNAME", "WORDPRESS_APP_PASSWORD"]
}
