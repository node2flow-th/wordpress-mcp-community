services:
  wordpress-mcp-community:
    build: .
    container_name: wordpress-mcp-community
    restart: unless-stopped
    ports:
      - "127.0.0.1:3023:3000"
    mem_limit: 512m
    mem_reservation: 256m
